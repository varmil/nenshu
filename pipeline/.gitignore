node_modules
coverage
*.tsbuildinfo

# APIキー
salary/.edinet_key
salary/.estat_key
salary/.gbiz_key

# 女性活躍DBのオープンデータ（手で落として置く。89MBのCSV・11MBのZIP。docs/worklife/spec.md 1.1）
worklife/source/

# パイプラインの生成物
salary/cache/
logo-cache/
salary/out/

# 分析用テキストの原文（C8・#240）。**194MB（gzip 54MB）あるので git に置かない。**
# `pipeline/data/` の最大である business_text_2026.csv でも15.2MB で、桁が違う。
# コミットするのは同じ工程が書く analysis_text_manifest_2026.csv（節ごとの字数と
# SHA-1）のほうで、原文は analysis/extract_analysis.py をキャッシュに対して回せば
# 27秒で作り直せる（EDINET を叩かない）。
data/analysis_text_2026.csv

# 生成の中間ファイル（C9・#241）。`generate.py clear` が消すのと同じもの。
# **成果物（company_analysis_2026.csv）はコミットする。**
analysis/work/
summary/work/
