{
  "name": "nenshu-data-pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "build:data": "tsx scripts/build-data.ts",
    "test": "vitest run && npm run test:py",
    "test:py": "python3 -m unittest discover -s salary -t salary -p 'test_*.py' && python3 -m unittest discover -s summary -t summary -p 'test_*.py'",
    "build:logos": "tsx scripts/build-logos.ts",
    "build:brand": "tsx scripts/build-brand.ts",
    "extract:worklife": "tsx worklife/extract.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "sharp": "^0.34.4"
  }
}
