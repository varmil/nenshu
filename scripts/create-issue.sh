#!/usr/bin/env bash
# docs/ranking/overview.md の Unit を GitHub Issue として起票する。
# 前提: gh auth login 済み、リポジトリのルートで実行。
#
#   bash scripts/create-issues.sh
#
# Issue が Unit の正になる。以降の作業はここを起点にする。

set -euo pipefail

gh label create "unit" --color "1D76DB" --description "Unit of Work" 2>/dev/null || true
gh label create "bolt-1" --color "0E8A16" --description "MVP: ランキング1ページ" 2>/dev/null || true

new() {
  gh issue create --title "$1" --label unit --label bolt-1 --body "$2"
}

# ── U0 ────────────────────────────────────────────────────────────────
new "[Unit] U0 データ変換パイプライン" "$(cat <<'EOF'
参照: `docs/adr/0003-age-conversion-client-side.md`, `docs/ranking/spec.md` §3

`scripts/build-data.ts` が、リポジトリ内のデータから配信用のJSONを生成する。

## 完了条件

- [ ] `data/ranking_unified_2026.csv` と `data/annual_curves.json` から `public/data/companies.json` と `public/data/curves.json` を生成する
- [ ] `companies.json` は1,867行を持ち、ADR-0003 の列指向スキーマに従う
- [ ] **カーブから再計算した35歳時点の推定年収が、CSVの `salary35` と全1,867社で一致する。** テストで固定する
- [ ] 補間の実装が Python 側（`salary35/curves.py` の `_interp`）と一致する。区分線形、範囲外は端の値で頭打ち
- [ ] `id` は証券コードを優先し、欠損する非上場107社は社名スラッグにする。1,867件すべて一意（社名の重複が2件ある）
- [ ] `companies.json` が gzip後100KB以内
- [ ] 産業大分類は内部キーとしてのみ持ち、表示用の値と混ざらない

## 非対象

UIの実装。JSONを作るところまで。

## 注意

有報の桁ミス（フジ日本・ヨロズ・クリアルが1,000倍、加藤製作所が10倍）はパイプライン側で補正済み。フロントで再補正しない。
EOF
)"

# ── U1 ────────────────────────────────────────────────────────────────
new "[Unit] U1 プロジェクト基盤とデザイントークン" "$(cat <<'EOF'
参照: `docs/adr/0001-hosting-cloudflare-pages.md`, `docs/adr/0002-stack-nextjs-static-export.md`, `CLAUDE.md`

中身が空でも構わないので、push したら公開URLが更新されるところまでを通す。

## 完了条件

- [ ] Next.js 15（App Router）が `output: 'export'` でビルドできる。TypeScript
- [ ] `web/design-system/tokens/tokens.css` に色・余白・タイポ・角丸・影のCSS変数がある
- [ ] `tailwind.preset.ts` がトークンをTailwindのテーマへ写像している
- [ ] shadcn/ui を導入し、`web/design-system/ui/` にプリミティブが入る
- [ ] **生の hex を書くと lint が落ちる。** Hooks で強制する
- [ ] 型チェックとテストが通らないとコミットできない
- [ ] main に push すると Cloudflare Pages が更新される
- [ ] `.gitignore` にAPIキー（`salary35/.edinet_key`, `salary35/.estat_key`）とパイプラインの生成物が入っている

## 非対象

ランキングの実装。
EOF
)"

# ── U2 ────────────────────────────────────────────────────────────────
new "[Unit] U2 ランキング表と年齢スイッチ" "$(cat <<'EOF'
参照: `docs/ranking/spec.md` §1.1〜1.4, AC-1 / AC-2 / AC-9
依存: U0, U1

施策の中核。1,867社を1つの表に並べ、年齢スイッチで金額を切り替える。

## 設計承認を厚く取る

**`useRankingState` の形をこのUnitで確定させる。** U3・U4・U5 がこの状態を共有するため、ここが割れると3ユニットぶんの手戻りになる。目標年齢・4つのフィルタ・検索語・表示件数を1か所で持ち、絞り込み後の行と順位を派生値として導出する形にする。実装前に設計を承認すること。

## 完了条件

- [ ] AC-1: 初期表示は35歳・絞り込みなしの上位100件。1位がキーエンスで推定年収2,178万円
- [ ] AC-2: 25歳を選ぶとキーエンスが1,642万円になる。ページ遷移も再取得も起きない
- [ ] AC-2後半: 60歳時点の上位50社に、35歳時点の上位50社が40社以上含まれる
- [ ] AC-9: 推定値であることを示す表記が常時ある。平均年収（補正前）と推定年収が同じ書式で並ばない
- [ ] **推定年収の列が視覚的に最も強い。** 順位ではなく金額が主役
- [ ] 推定年収の列見出しに選択中の年齢が入る
- [ ] モバイルではカード状の表示に切り替わる
- [ ] 年齢スイッチがキーボードで操作できる
- [ ] 操作から表の更新まで100ms以内

## 非対象

フィルタ（U3）、検索（U4）、URL同期（U5）。

## 注意

AC-2後半は仕様の性質をテストに変えたもの。補正倍率は業種ごとの共通因子なので、同業種内の順位は目標年齢に依存しない。これが崩れたら補間か業種の写像が壊れている。
EOF
)"

# ── U3 ────────────────────────────────────────────────────────────────
new "[Unit] U3 フィルタ4種" "$(cat <<'EOF'
参照: `docs/ranking/spec.md` §1.5, AC-3 / AC-4 / AC-5
依存: U2
共有: `useRankingState`（U2で確定した形を変えない）

## 完了条件

- [ ] 業種（東証33業種のプルダウン、単一選択）
- [ ] 従業員数: 〜300人（517社）/ 300〜1,000人（734社）/ 1,000人以上（616社）
- [ ] 在籍年数: 〜13年（545社）/ 13〜17年（732社）/ 17年以上（590社）
- [ ] 平均年齢: 〜40歳（450社）/ 40〜43歳（709社）/ 43歳以上（708社）
- [ ] 複数指定は AND で結合する
- [ ] 絞り込み後に順位を1から振り直す
- [ ] AC-3: 海運業を選ぶと7社になる
- [ ] AC-4: 1,000人以上を選ぶと616社になる
- [ ] AC-5: 業種と平均年齢を重ねると、業種のみより件数が減る
- [ ] キーボードで操作できる

## 非対象

複数選択、任意の列でのソート。
EOF
)"

# ── U4 ────────────────────────────────────────────────────────────────
new "[Unit] U4 フリーワード検索" "$(cat <<'EOF'
参照: `docs/ranking/spec.md` §1.6, AC-6
依存: U2
共有: `useRankingState`

会社名への部分一致。正規化してから照合する。

## 完了条件

- [ ] 全角英数→半角、半角カナ→全角カナ、英字→小文字、空白除去、法人格の除去（株式会社・(株)・㈱・有限会社・合同会社）
- [ ] AC-6: 「商船三井」で「株式会社　商船三井」が引ける（社名に全角スペースを含むものが74件ある）
- [ ] AC-6: 「ｷｰｴﾝｽ」で「株式会社キーエンス」が引ける
- [ ] フィルタと AND で結合する
- [ ] 入力から表の更新まで100ms以内。ネットワークアクセスを伴わない

## 非対象

業種名・業績での検索。あいまい検索。
EOF
)"

# ── U5 ────────────────────────────────────────────────────────────────
new "[Unit] U5 URLクエリとの同期" "$(cat <<'EOF'
参照: `docs/ranking/spec.md` §1.7, AC-7
依存: U3, U4
共有: `useRankingState`

## Bolt 2 のパスをここで決める

`/age/[age]`（8枚）、`/industry/[業種]`（33枚）、`/company/[key]`（1,867枚）を後から生やす。**このUnitでURLの形を確定させておけば、静的生成を足すだけで済む。** 決めた形は ADR に残すか `docs/ranking/overview.md` に追記する。

## 完了条件

- [ ] 年齢・4フィルタ・検索語がクエリパラメータに載る
- [ ] 初期値と同じ項目はクエリに出さない（初期状態のURLが `/` のまま）
- [ ] AC-7: `/?age=45&ind=銀行業` を直接開くと、45歳・銀行業82社の状態で復元される
- [ ] AC-7: ブラウザの戻るで一つ前の絞り込み状態に戻る
- [ ] URLを共有すると同じ絞り込みが再現される
- [ ] Bolt 2 のパス設計が文書に残っている

## 非対象

年齢別・業種別・企業詳細ページの実装そのもの。
EOF
)"

# ── U6 ────────────────────────────────────────────────────────────────
new "[Unit] U6 0件・端の状態と段階表示" "$(cat <<'EOF'
参照: `docs/ranking/spec.md` §1.8, AC-8
依存: U3, U4

## 完了条件

- [ ] AC-8: 0件のとき、条件を緩める案内が出る。エラー表示にはしない
- [ ] 初期表示は上位100件。以降はスクロールまたは追加読み込みで伸ばす
- [ ] 1,867行を一度に描画しない
- [ ] 読み込み中の状態がある

## 非対象

ページネーション（件数指定での移動）。
EOF
)"

# ── U7 ────────────────────────────────────────────────────────────────
new "[Unit] U7 計算方法ページ（/about）" "$(cat <<'EOF'
参照: `docs/ranking/spec.md` §2, AC-10
依存: U1

ロジックはない。文章が本体になる。既存サイトの弱点は根拠が見えないことにあり、方法の公開そのものが差別化になる（intent.md H3）。

## 完了条件

- [ ] 補正の式と、賃金カーブの作り方・出典
- [ ] 対象範囲: 2026年6〜7月提出の有報、単体従業員100人以上、1,867社
- [ ] 「本社のみ」バッジの意味。持株会社と事業会社で数百万円ずれる実例（みずほFG 1,166万 / みずほ銀行 870万 など）
- [ ] **年齢スイッチが同業種内の順位を動かさないこと**を明記する
- [ ] 限界の明示: 平均値であること、単体の数字であること、カーブが会社間の差から作られていること
- [ ] 出典（EDINET / 賃金構造基本統計調査）
- [ ] AC-10: ランキングページからリンクでたどれる

## 非対象

FAQ、運営者情報、プライバシーポリシー（収益化のタイミングで別途）。
EOF
)"

echo "8件のIssueを起票しました。U0 から着手してください。"