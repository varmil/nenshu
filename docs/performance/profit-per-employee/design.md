# design.md — P0 一人当たり経常利益の取り込み

Issue: [#155](https://github.com/varmil/nenshu/issues/155) ／ 親 [#154](https://github.com/varmil/nenshu/issues/154)
参照: `docs/performance/spec.md` 1.（データ）・AC-1〜AC-5

出来上がった内部構造。段取りは `plan.md`。

## 1書類に5期ぶん入っている（追加のダウンロードが要らなかった）

**`jpcrp_cor:OrdinaryIncomeLossSummaryOfBusinessResults` が `Prior4YearDuration` 〜 `CurrentYearDuration` の5つのコンテキストで来る。** 連結（接尾辞なし）と単体（`_NonConsolidatedMember`）の両方。

**直近5期の中央値は最新年の書類1件だけで出せる。** #155 が「T0 のキャッシュで足りるかを最初に確かめる」と書いていたのはここで、**足りた**。

**従業員数はそうではない。** `NumberOfEmployeesSummaryOfBusinessResults` に相当する要素は無く、当期の `jpcrp_cor:NumberOfEmployees` しかタグ付けされていない。5期ぶんは本文（`BusinessResultsOfGroupTextBlock`）の中にしかない。**年次の従業員数が要る P2（稼ぐ力の推移）だけが、10年ぶんの書類を取り直すことになる。**

## 計算

```
稼ぐ力 ＝ 連結の経常利益（直近5期の中央値） ÷ 連結の従業員数
```

- **連結で組む**（#154 の決定）。理由は `intent.md`。実測でも上位20社に「本社のみ」バッジが1社も無い
- **直近5期は「その会社が持つ年のうち新しい5つ」。** 固定の年で切らない——決算期は会社ごとにずれる
- **連結の従業員数が無い会社（191社）は単体で代用する**（`employeesConsolidated ?? employeesNonConsolidated`）。連結財務諸表を作っていない会社なので、単体がグループ全体でもある
- **連結の経常利益が無い年は単体で埋める**（`extract.py` の `_put` が基準の優先を見る）
- **赤字は負のまま持つ。** 捨てると「データが無い」と区別できなくなる

**同じ年が複数の書類から来る**（2026年の書類の `Prior1` と 2025年の書類の当期）。**遡りの少ないほう＝その年の書類の当期を優先する**——遡った値は会計基準の変更で組み替えられていることがあり、当期のほうが一次の記載になる。

## `performance.json` が持つのは金額と業種中央値だけ

**相対位置（レーダーの頂点をどこに打つか）の算出は表示側（P1）に置く。** 目盛りの決め方を後で変えてもデータを作り直さずに済む。

```
meta:           { count: 1867, matched: 1864, years: [2022..2026] }
perEmployee:    (number | null)[]   companies.rows と同じ並びの2,961件
industryMedian: (number | null)[]   companies.industries と同じ並びの33件
```

**`companies.rows` と同じ並びにする。** `stats.json`・`worklife.json` と同じ制約で、**行がずれると別の会社の稼ぐ力を出す。** 同じループで作り、テストで長さと代表値を固定してある。

## 測った

**2026-08-25 に E6（#182）で母集団 2,961社ぶんに取り直した。** 以下は取り直した後の値で、P0 当時（1,867社）の値は括弧で残す。

| 項目 | 実測値 | P0 当時（1,867社） |
| --- | ---: | ---: |
| 経常利益を取れた会社 | **2,959社（99.9%）** | 1,865社（99.9%） |
| 5期そろう | 2,950社 | 1,855社 |
| 1期しか無い | 0社 | 1社 |
| 連結の従業員数で割った | 2,590社 | 1,675社 |
| 単体で代用した | 371社 | 189社 |
| **5期の中央値が0以下（赤字）** | **136社** | 59社 |
| `performance.json`（gzip） | **11.4KB**（上限32KB） | 7.2KB |
| `profit-history.json`（gzip） | **289.5KB**（上限を 256 → **384KB** に上げた） | 187.0KB |

**取れない2社は増えていない**（弘電社・キクカワエンタープライズ）。最後に開示したのが8年前で、「直近5期」が2014〜2018年になってしまう会社。**母集団が1.59倍になっても2社のまま**で、これは新しく入った会社が最近の開示を持っているため。

**`profit-history.json` の上限だけを上げた。** 社数が1.59倍になったぶんそのままで、1社あたりは変わっていない。**何を切り出すかは Worker バンドルが 3MiB に近づいたかで決める**（`worklife.json` と同じ扱い）——E6 の実測でバンドルは gzip **2.17MB（68.9%）**。

**P2（#168）で10年ぶんの書類を取ったあと、54社で値が変わり、電気機器の業種中央値は 181万円 → 191万円 になった。** 「遡りの少ないほう＝その年の書類の当期を優先する」が効くようになったためで、精度が上がった変更になる。**要素名の取り違えも2つ見つかって直した**（`docs/performance/profit-trend/design.md` の「取り込みで直したもの」）。

### 業種別のカバー率（AC-2）

**銀行業82社・保険業17社・その他金融業40社とも欠損なし**（P0 当時は 82・13・33社）。営業利益が無いことを理由に欠ける会社は出なかった——経常利益は業種を問わず「主要な経営指標等の推移」に載る。

### 上位20社に持株会社は1社も無い（AC-2 の狙い）

| 会社 | 稼ぐ力 | 業種 |
| --- | ---: | --- |
| ＩＮＰＥＸ | 1億7,678万円 | 鉱業 |
| 全国保証 | 1億1,000万円 | その他金融業 |
| 国際協力銀行 | 9,332万円 | その他金融業 |
| 三共 | 7,150万円 | 機械 |
| 東京都競馬 | 7,026万円 | サービス業 |
| 任天堂 | 6,936万円 | その他製品 |
| 日本政策投資銀行 | 6,925万円 | その他金融業 |
| 中外製薬 | 6,669万円 | 医薬品 |

**「本社のみ」バッジが付いた会社は上位20社に入っていない。E6 で母集団を1.59倍にしても0社のまま。** 単体で組むと分子が実質的に子会社からの配当になり上位が持株会社で埋まる——連結で組むという判断は、例外処理を1つも足さずにこれを避けている。

**1位が入れ替わった**（全国保証 → ＩＮＰＥＸ 1億7,678万円）。ＩＮＰＥＸは12月期決算で、取得の窓を直近12か月に広げた E2（#173・ADR-0011）で入った会社。

### 業種差は30.7倍（中央値の併記が要る）

| 業種 | 中央値 | | 業種 | 中央値 |
| --- | ---: | --- | --- | ---: |
| 鉱業 | 3,846万円 | | 電気機器 | 214万円 |
| 海運業 | 1,232万円 | | 情報・通信業 | 164万円 |
| その他金融業 | 1,126万円 | | サービス業 | 148万円 |
| 銀行業 | 772万円 | | 空運業 | 147万円 |
| 電気・ガス業 | 675万円 | | 輸送用機器 | 141万円 |
| | | | 陸運業 | 125万円 |

**どの業種が端に来るかは母集団で入れ替わる。** P0 当時（1,867社）は 海運業2,524万 / 輸送用機器131万 の19倍だったが、E6 で広げた後の端は 鉱業3,846万 / 陸運業125万 の **30.7倍**。**併記が要るという結論は変わらない**が、**業種名を決め打ちして「19倍」と書くとこういうときに嘘になる**ので、`build-data.test.ts` は端そのもの（最大 ÷ 最小）を見るようにした。

**中央値を採るのは1社の外れ値に引きずられないため。** 電気機器はキーエンスが業種中央値の18.9倍で、平均を使うと業種の代表値がキーエンスに引きずられる。

### 業種中央値に対する倍率の分布（P1 が目盛りを決めるための材料）

| 1%tile | 5%tile | 25%tile | 中央 | 75%tile | 95%tile | 99%tile |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| −1.32 | 0.03 | 0.50 | 1.00 | 1.79 | 4.73 | 11.09 |

**10倍超が37社、0.1倍未満が62社、負が136社**（P0 当時は 25・97・59社。分布の形は変わっていない）。線形に当てると上位が振り切れ、下位が中心に潰れる。**相対位置は順位で決めた**（P1・`../company-radar/design.md`）。

### 臨時雇用人員は従業員数に入らない

小売業の上位はワークマン（5,660万円）・ZOZO・セリアで、**フランチャイズ型・EC型で正社員が少ない業態**が並ぶ。有報の従業員数は正社員等の数で、パート・アルバイトは年間平均人員の外書きになる。

**値そのものは正しい**（実際に少人数で高収益）が、店頭の人手を含まない数字であることは画面に断る（P1）。

## ファイル

| ファイル | 役割 |
| --- | --- |
| `pipeline/performance/fetch.py` | 有報のZIPを取得。`salary_history.csv` の `doc_id` から落とす |
| `pipeline/performance/extract.py` | 「主要な経営指標等の推移」から経常利益を抜き、long CSV にする |
| `pipeline/data/performance_history.csv` | 中間成果物（9,305行）。コミットする |
| `pipeline/scripts/lib/csv.ts` | `employeesConsolidated` の追加と `parsePerformanceHistoryCsv` |
| `pipeline/scripts/build-data.ts` | `buildPerformance()`。gzip 上限32KB |
| `pipeline/scripts/build-data.test.ts` | AC-1〜AC-4 |

## 踏んだもの

- **キャッシュ（`pipeline/salary/cache/`・1.4GB）は `.gitignore` 済み。** まっさらなコンテナでは空から始まるので、取得に **2026年ぶん1,867件で約17分**（並列3・実測1.8件/秒）かかった。10年ぶん17,684件なら約150分になる
- **`salary_history.csv` に10年ぶんの `doc_id` が入っている。** 書類一覧（`list_*.json`）はキャッシュが無いと引き直しになるが、**この CSV があればその工程を丸ごと飛ばせる**
