# spec.md — 働きやすさ指標

対象面: user-web（ユーザー画面）

親 Issue: #148 ／ Unit: W0 #149・W1 #150

厚生労働省「女性の活躍推進企業データベース」から**平均残業時間・年次有給休暇の取得率・男女の賃金の差異**を取り込み、企業詳細ページ `/company/[id]` に出す。用語は `docs/product/glossary.md` に従う。突合の方式は ADR-0009 に従う。

数値はすべて `4e6b3d07-99_20260820_utf8_bom.zip`（2026-08-20 生成・全件版）での実測値。

---

## 1. データ

### 1.1 出典と取得

厚生労働省「女性の活躍推進企業データベース」オープンデータ（https://positive-ryouritsu.mhlw.go.jp/positivedb/opendata/ ）の全件版。CSV・UTF-8・ZIP 圧縮で、BOM 有りと無しの2種類が配られている。

**BOM 無しの版を読む。** BOM 有りは Excel 向けで、法人番号が数値として解釈され先頭の0が落ちうる。

**取得は自動化しない。手元に置いた ZIP を読む。** ダウンロードURLに `4e6b3d07-` のような UUID が入っていて固定できない。更新は年1回（有報の再取得と同時）を既定とし、**ファイル名・sha256・取得日を manifest に残す**。

**再配布の可否は運営者が判断する**（`docs/product/product.md` の制約）。このデータベースには独自の「オープンデータ利用規約」がある。

### 1.2 突合

**法人番号（13桁）で内部結合する。ADR-0009。**

- 有報側 — `ranking_unified_2026.csv` に `corporate_number` 列を足す。値は EDINETコードリスト（`Edinetcode.zip`）の `提出者法人番号`
- 女性活躍DB側 — 2列目の `法人番号`

**証券コードと社名では突合しない**（理由は ADR-0009）。**一致しない会社は「データ無し」として扱い、子会社や同名の別会社で代用しない。**

同じ法人番号が複数行にある場合（全体で7件）は**最終更新日が新しい行**を採る。

### 1.3 取り込む項目

| 女性活躍DB の列 | 取り込む形 |
| --- | --- |
| 6. 一月当たりの労働者の平均残業時間 — 公表する範囲 / 平均残業時間(時間) | 全体値とそのスコープ |
| 7. 雇用管理区分ごとの一月当たりの労働者の平均残業時間 — 区分1〜5 | 区分名と値の組（最大5） |
| 8.(1) 年次有給休暇の取得率 — 対象労働者(%) | 全体値 |
| 8.(2) 年次有給休暇の取得率(区) — 区分1〜5 | 区分名と値の組（最大5） |
| 14. 男女の賃金の差異 1/2/3 — 全労働者 / うち正規雇用労働者 / うち非正規雇用労働者(%) | 3つの値 |
| 14. 男女の賃金の差異 注釈・説明 | 自由記述（716社） |
| 14. 対象期間 | 文字列（`2025年4月1日～2026年3月31日` 等） |
| 16. データ集計時点 | 文字列（`2026年3月時点` 等） |
| データの最終更新日 | 文字列 |

**236列のうちこれ以外は取り込まない**（intent.md「作らないもの」）。

**列は名前の完全一致で検証してから読む。** 厚労省が列を足したときに黙って添字がずれると、別の指標を残業時間として出す。`parseUnifiedCsv` と同じく、想定と違えば例外で落とす。

**列名には重複がある**（`-女性(%)` 等が5回繰り返される）。**`DictReader` 相当の名前引きを使わず、位置で読む。**

### 1.4 値の扱い

**欠測と 0 を分ける。** 「未登録」と「残業0時間」は別物で、`null` と `0` として区別する。

**雇用管理区分は畳まない。** 区分名と値の組をそのまま持つ。**平均して1つの値にしない**——区分は自由記述で、「正社員」と「派遣社員」と「アルバイト」が同じ列に並ぶ。

**異常値の扱いを個別に決める。**

| 実測された値 | 扱い |
| --- | --- |
| 平均残業時間 `-16.8`（1社） | **落とす。** 負の残業時間は入力ミス以外にありえない |
| 有給取得率 100超（7社。小松製作所103.0 ほか） | **残す。** 前年繰越分の消化で100%を超えるのは正しい |
| 男女の賃金の差異 638.6（1社） | **残す。** 少人数区分の外れ値だが値としては正しい |
| `-`（非正規で51社） | 欠測として `null` |

**落とした値は件数と会社名をビルド時のサマリーに出す。** 黙って消さない。

### 1.5 出力

`pipeline/data/worklife_2026.csv`（突合済み・1社1行）を中間成果物としてコミットし、`build-data.ts` が `web/public/data/worklife.json` を作る。

**`worklife.json` は `/company/[id]` だけが import する。`app/page.tsx` からは読まない**——トップページのHTML（gzip 64KB・Issue #22）を1バイトも増やさないため。

**表現は `companies.json` と同じ文字列プール＋フラットな配列にする。** 入れ子の構造にすると起動時のオブジェクト生成が増える（Node 22 での実測: 入れ子 697KB で 30.8ms、フラット 250KB で 12.5ms。`companies.json` は 133KB で 6.2ms）。区分名は重複が多く（「正社員」187社・「総合職」87社）、プール化すると1,322語に収まる。

---

## 2. 表示（企業詳細ページ）

### 2.1 置き場所

`/company/[id]` に「働きやすさ」の節を1つ足す。**推移（timeseries）と同じ扱いで、表示基準（実測値 / 年齢そろえ）とは独立に出す**——年齢補正を通さない値なので、年齢スイッチでも表示基準スイッチでも変わらない。

### 2.2 平均残業時間

全体値がある会社は `◯◯.◯時間 / 月` と、その**公表する範囲**（`対象正社員` / `その他` / `基幹的な職種`）を添える。

雇用管理区分ごとの値がある会社は、**区分名と値を並べて出す**（三菱商事なら 総合職14.1時間・一般職3.3時間）。全体値と区分別の両方がある会社は両方出す。

### 2.3 年次有給休暇の取得率

全体値がある会社は `◯◯.◯%`。区分ごとの値がある会社は区分名と並べて出す。

**100%を超える値をそのまま出す。** 丸めない。繰越分の消化で超えることを注記する。

### 2.4 男女の賃金の差異

全労働者・うち正規雇用労働者・うち非正規雇用労働者の3つを出す。

**値の意味を必ず併記する** ——`女性の平均賃金 ÷ 男性の平均賃金 × 100`。100未満は女性のほうが低いことを示す。

**数字だけを単独で置かない。** 差異の主因が職種構成や勤続年数の差であることが多い旨の共通の注記を添える。**会社が注釈・説明を登録している場合（716社）はそれも出す。**

### 2.5 出典と時点

節の中に必ず置く。

- 出典 — 厚生労働省「女性の活躍推進企業データベース」
- **企業の自己申告値であり、有価証券報告書のような監査を経ていない**こと
- その会社の**データ集計時点**（`2026年3月時点` 等）と、賃金の差異の**対象期間**
- 有報の決算期（`2026年3月期`）とは**別の時点**であること

**「推定」の語は使わない。** 推定値ではない。**「実測値」とも呼ばない**——`docs/product/glossary.md` の「実測値」は有報の平均年間給与を指す語で、意味が衝突する。**「自己申告値」と呼ぶ。**

### 2.6 データが無いとき

**節ごと消さない。** 3指標のいずれも無い会社（突合1,679社のうち144社、未突合188社を含めて全1,867社中332社）にも節は出し、**掲載が無いことと、その理由（データベースへの掲載は任意）を書く。**

3つのうち一部だけ無い会社も同じで、**無い指標は「掲載なし」と出す**。項目を消すと「残業が少ない会社」と見分けがつかない。

### 2.7 対象外

働きやすさランキング、順位・偏差値・スコア、総合評価、女性活躍DBの他の項目（管理職の女性比率・育児休業取得率・えるぼし認定等）、若者雇用促進総合サイトのデータ。いずれも `docs/worklife/intent.md`「作らないもの」を参照。

**ランキングページには出さない**（Issue #22）。

---

## 3. 非機能要件

**配信** — 企業詳細ページと同じフルSSR（ADR-0004）。エッジキャッシュを効かせる。

**データ量** — `worklife.json` の gzip サイズに上限を置き、`build-data.ts` で超過したらビルドを落とす（`companies.json` の100KB制限と同じ扱い）。**トップページのペイロードは1バイトも増やさない。**

**計算量** — リクエスト時の処理は当該1社ぶんの引き当てだけにする。全社の集計をリクエストごとに回さない（Workers Free の CPU 10ms/リクエスト制約）。

**起動コスト** — `worklife.json` の import によるモジュール評価時間を実測し、`docs/worklife/<unit>/design.md` に残す。DuckDB・D1 は使わない（1,679行の読み取り専用データに DB を持ち込まない。将来 Issue #22・#54 の規模で再検討する）。

**対応環境** — モバイルを主とする。横スクロールを発生させない。雇用管理区分が5つある会社でも崩れないこと。

**SEO** — 数値はすべて初期HTMLに含める。

**アクセシビリティ** — 区分別の値は表として読み上げられる形にする。

---

## 4. 受け入れ基準

### AC-1 突合

```gherkin
Given pipeline/data/ranking_unified_2026.csv に corporate_number 列がある
When 女性活躍DBの全件CSVと法人番号で内部結合する
Then 1,679社が突合できる
And 突合率・指標ごとの記入率がサマリーに出力される
```

```gherkin
Given 阪急阪神ホールディングス株式会社（証券コード 9042）
When 突合する
Then 阪神電気鉄道株式会社の行が紐づかない
```

```gherkin
Given 株式会社三菱ＵＦＪフィナンシャル・グループ（持株会社）
When 突合する
Then データ無しになる
And 傘下の事業会社の値が代用されない
```

### AC-2 列の検証

```gherkin
Given 女性活躍DBのCSVの列見出しが想定と1つでも違う
When 取り込みを実行する
Then 例外で停止する
And 読み進めない
```

### AC-3 値の扱い

```gherkin
Given ビジネスエンジニアリング株式会社の平均残業時間が -16.8 である
When 取り込む
Then その値は落とされる
And 落とした件数と会社名がサマリーに出る
```

```gherkin
Given 株式会社小松製作所の有給休暇取得率が 103.0 である
When 取り込む
Then 103.0 のまま残る
```

```gherkin
Given 三菱商事株式会社の雇用管理区分ごとの残業時間が 総合職14.1 / 一般職3.3 である
When 取り込む
Then 2つの組がそのまま保持される
And 平均された1つの値になっていない
```

### AC-4 出力

```gherkin
Given 取り込みが完了している
When npm run build:data を実行する
Then web/public/data/worklife.json が出力される
And gzip サイズが上限以内である
And 上限を超えたらビルドが落ちる
```

```gherkin
Given トップページのHTML
When 取り込みの前後で比較する
Then gzip サイズが変わらない
```

### AC-5 表示に影響しない（W0 の完了条件）

```gherkin
Given W0 のみを適用した状態
When 既存の Unit テストと E2E を実行する
Then すべて緑のままである
And 画面の表示が1つも変わらない
```

### AC-6 平均残業時間

```gherkin
Given /company/7203 を開く
Then 「働きやすさ」の節に平均残業時間 20.3時間 / 月 が表示される
And 公表する範囲が「対象正社員」であることが読める
```

```gherkin
Given /company/8058 を開く
Then 平均残業時間 10.5時間 / 月（その他）が表示される
And 雇用管理区分ごとの値として 総合職 14.1時間・一般職 3.3時間 が表示される
```

### AC-7 年次有給休暇の取得率

```gherkin
Given /company/4689 を開く
Then 年次有給休暇の取得率 87.4% が表示される
```

```gherkin
Given /company/6861 を開く
Then 年次有給休暇の取得率が「正社員 38.8%」として表示される
```

### AC-8 男女の賃金の差異

```gherkin
Given /company/7203 を開く
Then 男女の賃金の差異が 全労働者 67.0% / うち正規雇用労働者 66.8% / うち非正規雇用労働者 59.7% と表示される
And 「女性の平均賃金 ÷ 男性の平均賃金 × 100」である旨が読める
And 差異の主因が職種構成や勤続年数の差であることが多い旨の注記がある
```

```gherkin
Given 会社が注釈・説明を登録している
Then その本文が同じ節に表示される
```

### AC-9 出典と時点

```gherkin
Given /company/7203 の「働きやすさ」の節
Then 出典が厚生労働省「女性の活躍推進企業データベース」と書かれている
And 企業の自己申告値であり監査を経ていない旨が書かれている
And データ集計時点「2026年3月時点」が表示される
And 賃金の差異の対象期間「2025年4月1日～2026年3月31日」が表示される
```

```gherkin
Given 「働きやすさ」の節
Then 「推定」の語が存在しない
And 「実測値」の語が存在しない（有報の平均年間給与を指す語なので使わない）
```

### AC-10 データが無いとき

```gherkin
Given /company/6861 を開く（キーエンスは残業時間を登録していない）
Then 「働きやすさ」の節は表示される
And 平均残業時間が「掲載なし」と表示される
And 有給取得率と男女の賃金の差異は表示される
```

```gherkin
Given 女性活躍DBに掲載が無い会社（例: /company/8306 三菱UFJフィナンシャル・グループ）
Then 「働きやすさ」の節は表示される
And 掲載が無いことと、データベースへの掲載が任意である旨が書かれている
```

### AC-11 表示基準と独立

```gherkin
Given /company/7203 を開いている
When 「見せ方」を「年齢そろえ」に切り替える
Then 働きやすさの3指標の値が1つも変わらない
And ページ遷移が発生しない
```

```gherkin
Given /company/7203?age=60 を直接開く
Then 働きやすさの値が /company/7203 と同じである
```

### AC-12 レイアウトと初期HTML

```gherkin
Given 390px 幅で /company/8058 を開く
Then 横スクロールが発生しない
And 雇用管理区分ごとの値が読める
```

```gherkin
Given /company/7203 に対するHTMLをJS実行前に取得する
Then 「20.3」「67.0」がHTMLに含まれている
```

---

## 5. 未決事項

- **「働きやすさ」という節見出しの語。** 3指標が働きやすさを代表しているわけではない（残業と有給は労働時間、賃金の差異は処遇の話）。C2 で決めたページ構成の中でどこに置くかと合わせて W1 で決める。
- **若者雇用促進総合サイトの調達可否。** 卒3年以内離職率・平均継続勤務年数が取れる見込みだが、オープンデータの形で配布されているかを確認していない。取り込むならこの施策に合流させる。
- **平均継続勤務年数を出すなら、有報の平均勤続年数（在籍年数）と重複する。** 定義が違う（有報は提出会社単体・監査済み、若者雇用は自己申告）ので、両方出すか片方に寄せるかを先に決める。
